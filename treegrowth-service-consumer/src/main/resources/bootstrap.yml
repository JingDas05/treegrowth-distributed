spring:
  application:
    name: service-consumer
#  cloud:
#    config:
#      uri: http://localhost:0001/
##      profile: dev

server:
  port: 3333

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1111/eureka/

kafka:
  broker:
    address: localhost:9092
  topic: treegrowth
